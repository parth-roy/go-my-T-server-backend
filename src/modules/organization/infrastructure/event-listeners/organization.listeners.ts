/**
 * organization.listeners.ts
 *
 * This file registers event listeners for the Organization domain using the
 * existing global event bus (@shared/eventbus).
 *
 * Currently, there are no event listeners required for the Organization domain.
 * When needed, import `eventBus` from `@shared/eventbus` and attach handlers here.
 */

export function registerOrganizationListeners() {
  // No listeners required currently.
  // Example: eventBus.on('organization.created', async (data) => { ... });
}
